A command to rename the cloud volume with the "ex4mp1eID" ID:

```
srvctl cloud-volumes update ex4mp1eID --name new-name
```

A command to assign labels for the cloud volume with the "ex4mp1eID" ID:

```
srvctl cloud-volumes update ex4mp1eID --label environment=production --label team=frontend
```
