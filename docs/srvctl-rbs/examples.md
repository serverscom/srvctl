A command to list all remote block storage volumes for the account:

```
srvctl rbs list
```

A command to get information for a specific remote block storage volume:

```
srvctl rbs get ex4mp1eID
```