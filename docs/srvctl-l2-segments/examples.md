A command to list all L2 segments for the account:

```
srvctl l2-segments list
```

A command to get information for a specific L2 segment:

```
srvctl l2-segments get <l2-segment-id> 
```

A command to delete an L2 segment:

```
srvctl l2-segments delete <l2-segment-id>
```
