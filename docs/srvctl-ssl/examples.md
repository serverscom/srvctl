A command to list all SSL certificates for the account:

```
srvctl ssl list
```

A command to list custom SSL certificates:

```
srvctl ssl custom list
```

A command to list Let's Encrypt SSL certificates:

```
srvctl ssl le list
```
