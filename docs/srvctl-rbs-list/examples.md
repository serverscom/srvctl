A command to list all remote block storage volumes for the account:

```
srvctl rbs list
```

A command to filter remote block storage volumes by label:

```
srvctl rbs list --label-selector environment=production
```

A command to fetch all pages of remote block storage volumes:

```
srvctl rbs list 
```