A command to activate rescue mode for the cloud instance with the "ex4mp1eID" ID:

```
srvctl cloud-instances rescue-mode ex4mp1eID --command activate
```

A command to deactivate rescue mode for the cloud instance with the "ex4mp1eID" ID:

```
srvctl cloud-instances rescue-mode ex4mp1eID --command deactivate
```
