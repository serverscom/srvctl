A command to list all cloud instances for the account:

```
srvctl cloud-instances list
```

A command to get information for a specific cloud instance:

```
srvctl cloud-instances get ex4mp1eID
```

A command to delete a cloud instance:

```
srvctl cloud-instances delete ex4mp1eID
```
