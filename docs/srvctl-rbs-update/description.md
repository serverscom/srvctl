This command updates parameters and labels for the selected remote block storage volume. Use `--help` to see available flags.

Parameters can be passed via flags or via a file using the `-i` or `--input` flag.