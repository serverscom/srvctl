A command to update the title of the network pool with the "ex4mp1eID" ID:

```
srvctl network-pools update ex4mp1eID --title "New title"
```

A command to update labels for the network pool:

```
srvctl network-pools update ex4mp1eID --label env=prod --label team=backend
```
