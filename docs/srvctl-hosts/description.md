A host is a bare metal-based service that includes enterprise bare metal, kubernetes bare metal nodes and scalable bare metal. Use `srvctl hosts list` to list hosts of all types; manage a specific type with the dedicated `ebm`, `kbm` and `sbm` commands.
