A command to update the title of the subnetwork with the "ex4mp1eSubnetID" ID in the network pool with the "ex4mp1ePoolID" ID:

```
srvctl network-pools update-subnet ex4mp1ePoolID --network-id ex4mp1eSubnetID --title "Updated subnet"
```
