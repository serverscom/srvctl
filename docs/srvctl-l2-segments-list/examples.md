A command to list all L2 segments for the account:

```
srvctl l2-segments list 
```

A command to filter L2 segments by label:

```
srvctl l2-segments list --label-selector environment=production
```
