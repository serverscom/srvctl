A command to power on the cloud instance with the "ex4mp1eID" ID:

```
srvctl cloud-instances power ex4mp1eID --command on
```

A command to power off the cloud instance with the "ex4mp1eID" ID:

```
srvctl cloud-instances power ex4mp1eID --command off
```
